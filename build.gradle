plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.3.71' apply true
}

group 'org.kotlinhyd.petstore'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

kotlin {
    jvm()
    js {
        browser {
        }
    }
}