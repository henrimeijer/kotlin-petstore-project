server:
  port: 9090

spring:
  datasource:
    url: jdbc:h2:file:~/data/petstoredb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect